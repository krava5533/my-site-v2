# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# local persistent data (JSON content store + uploaded files) —
# lives outside /public and /lib so a single mounted volume at
# /storage covers everything on hosts that only allow one volume
/storage/

# prisma
/prisma/dev.db
